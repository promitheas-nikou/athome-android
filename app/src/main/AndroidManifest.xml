<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AtHome"
        tools:targetApi="31"
        android:usesCleartextTraffic="true">
        <activity
            android:name="com.example.athome.activities.SettingsActivity"
            android:label="@string/app_name" />
        <activity
            android:name="com.example.athome.activities.AboutActivity"
            android:label="@string/app_name" />
        <activity
            android:name="com.example.athome.activities.BookNewHairdresserArrangementActivity"
            android:label="@string/app_name" />
        <activity
            android:name="com.example.athome.activities.BookNewHairdresserArrangementFinalizationActivity"
            android:label="@string/app_name" />
        <activity
            android:name="com.example.athome.activities.HairdresserArrangementReviewActivity"
            android:label="@string/app_name" />
        <activity
            android:name="com.example.athome.activities.BookNewArrangementResultsViewActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AtHome.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>